<%= form_with(model: watching, local: true) do |form| %>
  <% if watching.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(watching.errors.count, "error") %> prohibited this watching from being saved:</h2>

      <ul>
        <% watching.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :issue_id %>
    <%= form.number_field :issue_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
